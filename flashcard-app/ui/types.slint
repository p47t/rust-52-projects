export struct DeckInfo {
    name: string,
    description: string,
    card-count: int,
    due-count: int,
}

export struct DeckStats {
    total-cards: int,
    due-today: int,
    total-reviews: int,
    correct-reviews: int,
    average-ease: float,
}
